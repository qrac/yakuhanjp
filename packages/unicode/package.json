{
  "name": "unicode",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run clean && npm run build:src",
    "build:src": "tsx ./src/build.ts",
    "clean": "rimraf ./dist"
  }
}
